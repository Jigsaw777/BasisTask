package com.example.basistask.interfaces;

//I haven't used navigation component as I'm still learning it. So, for going from 1 fragment to the other, I have used this interface
public interface FragmentCommunication {
     void startFragment(String fragmentName);
}
